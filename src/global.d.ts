// Build-time constants injected by vite (see vite.config.ts `define`).
// Surfaced in Settings (FIX_PLAN A-10) so users can report which build a
// bug came from.

declare const __APP_VERSION__: string;
declare const __GIT_SHA__: string;
